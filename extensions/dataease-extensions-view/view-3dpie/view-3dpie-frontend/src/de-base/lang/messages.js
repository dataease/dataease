import enLocale from './en'
import zhLocale from './zh'
import twLocale from './tw'

const messages = {
    en_US: {
      ...enLocale
    },
    zh_CN: {
      ...zhLocale
    },
    zh_TW: {
      ...twLocale
    }
}

export default messages